import React,{useEffect,useMemo,useState}from'react';
import{Home,BookOpen,ClipboardCheck,Settings,Plus,Search,Moon,Sun,GraduationCap,FileText,Menu,X,ChevronLeft,User,Building2,ArrowLeft,LogIn,LogOut,CheckCircle2,Clock3,RefreshCw,CalendarDays,History,FolderOpen,Trash2}from'lucide-react';
const uid=()=>crypto.randomUUID?.()||String(Date.now());
const seed=[{id:'1',name:'مهارات التخصص',code:'COURSE 101',chapters:12,done:8,review:2},{id:'2',name:'المهارات الأكاديمية',code:'COURSE 102',chapters:10,done:4,review:2},{id:'3',name:'مقرر اختياري',code:'COURSE 103',chapters:8,done:2,review:1}];
export default function App(){const[stage,setStage]=useState(()=>localStorage.getItem('novi-onboarded')?'app':'login'),[page,setPage]=useState('home'),[dark,setDark]=useState(localStorage.getItem('novi-theme')==='dark'),[courses,setCourses]=useState(()=>JSON.parse(localStorage.getItem('novi-courses')||'null')||seed),[q,setQ]=useState(''),[menu,setMenu]=useState(false);useEffect(()=>{document.documentElement.dataset.theme=dark?'dark':'light';localStorage.setItem('novi-theme',dark?'dark':'light')},[dark]);useEffect(()=>localStorage.setItem('novi-courses',JSON.stringify(courses)),[courses]);const filtered=useMemo(()=>courses.filter(c=>c.name.includes(q)||c.code.toLowerCase().includes(q.toLowerCase())),[courses,q]);if(stage!=='app')return <Auth stage={stage} setStage={setStage}/>;const nav=[['home','لوحة التحكم',Home],['courses','المقررات',BookOpen],['tests','مركز الاختبارات',ClipboardCheck],['files','الملاحظات والملفات',FolderOpen],['settings','الإعدادات',Settings]];function add(){let n=prompt('اسم المقرر');if(!n?.trim())return;setCourses(v=>[...v,{id:uid(),name:n.trim(),code:'NEW COURSE',chapters:0,done:0,review:0}])}function remove(id,name){if(window.confirm(`هل أنت متأكد من حذف مقرر «${name}»؟`))setCourses(v=>v.filter(c=>c.id!==id))}function logout(){localStorage.removeItem('novi-onboarded');setMenu(false);setStage('login')}return <div className="shell" dir="rtl"><aside className={menu?'open':''}><div className="logo"><span>◈</span><b>NoviStudy</b><button onClick={()=>setMenu(false)}><X/></button></div><div className="profile"><div className="avatar">ط</div><div><b>مرحباً بك</b><small>مساحتك الدراسية</small></div></div><nav>{nav.map(([id,t,I])=><button key={id} className={page===id?'active':''} onClick={()=>{setPage(id);setMenu(false)}}><I/>{t}</button>)}<button className="logoutBtn" onClick={logout}><LogOut/>تسجيل الخروج</button></nav><div className="themeMini"><button onClick={()=>setDark(v=>!v)}>{dark?<Sun/>:<Moon/>}</button><span>الوضع {dark?'الفاتح':'الليلي'}</span></div><div className="quote">خطط • تعلّم • أنجز</div></aside><main><header><button className="mobile" onClick={()=>setMenu(true)}><Menu/></button><div className="search"><Search/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="ابحث عن مقرر، شابتر، أو ملاحظة..."/></div><button className="round" onClick={()=>setDark(v=>!v)}>{dark?<Sun/>:<Moon/>}</button></header>{page==='home'&&<HomePage courses={courses} go={setPage}/>} {page==='courses'&&<Courses courses={filtered} add={add} remove={remove}/>} {page==='tests'&&<Tests/>}{page==='files'&&<EmptyPage title="الملاحظات والملفات"/>}{page==='settings'&&<SettingsPage dark={dark} setDark={setDark}/>}</main></div>}
function Auth({stage,setStage}){const[name,setName]=useState(''),[major,setMajor]=useState(''),[uni,setUni]=useState('');const enter=()=>{localStorage.setItem('novi-onboarded','1');localStorage.setItem('novi-profile',JSON.stringify({name,major,uni}));setStage('app')};return <div className="auth" dir="rtl"><section className="authBrand"><div className="brandLogo"><span>✦</span><div><b>NoviStudy</b><small>Plan • Learn • Achieve</small></div></div><div className="brandPitch"><h1>رحلتك الأكاديمية<br/><em>تبدأ من هنا</em></h1><p>منصة متكاملة لكل الطلاب في جميع التخصصات، تساعدك على تنظيم المقررات والاستعداد للاختبارات.</p></div></section><section className="authSide">{stage==='login'?<div className="authCard"><div className="authMark">✦</div><h2>مرحباً بك في NoviStudy</h2><p>سجّل الدخول وابدأ تنظيم رحلتك الدراسية</p><button className="social apple" onClick={()=>setStage('profile')}> <b>المتابعة باستخدام Apple</b></button><button className="social google" onClick={()=>setStage('profile')}><b>G</b><span>المتابعة باستخدام Google</span></button><button className="authPrimary" onClick={()=>setStage('profile')}><LogIn/>تسجيل الدخول</button></div>:<div className="authCard profileCard"><div className="authMark">✦</div><h2>أكمل بياناتك الأكاديمية</h2><label>اسم المستخدم<div className="field"><User/><input value={name} onChange={e=>setName(e.target.value)} placeholder="مثال: أحمد محمد"/></div></label><label>التخصص الدراسي<div className="field"><GraduationCap/><input value={major} onChange={e=>setMajor(e.target.value)} placeholder="مثال: الطب، الهندسة، القانون..."/></div></label><label>الجامعة أو الكلية<div className="field"><Building2/><input value={uni} onChange={e=>setUni(e.target.value)} placeholder="اسم الجامعة أو الجهة التعليمية"/></div></label><button className="authPrimary" onClick={enter}>ابدأ رحلتك الدراسية <ArrowLeft/></button></div>}</section></div>}
function HomePage({courses,go}){const total=courses.reduce((a,c)=>a+(+c.chapters||0),0),done=courses.reduce((a,c)=>a+(+c.done||0),0),review=courses.reduce((a,c)=>a+(+c.review||0),0),pending=Math.max(0,total-done-review),progress=total?Math.round(done/total*100):0;return <><section className="motivation"><div><h1>كل خطوة صغيرة تقرّبك من هدفك الكبير.</h1><p>استمر... أنت تبني مستقبلك الآن.</p></div><GraduationCap/></section><section className="stats five"><Stat icon={RefreshCw} n={progress+'%'} t="إجمالي نسبة الإنجاز"/><Stat icon={BookOpen} n={total} t="إجمالي الشباتر"/><Stat icon={CheckCircle2} n={done} t="انتهيت منها" tone="green"/><Stat icon={Clock3} n={pending} t="لم تكتمل" tone="orange"/><Stat icon={RefreshCw} n={review} t="تحتاج إلى مراجعة" tone="red"/></section><ExamCards/><div className="dashGrid"><section className="panel"><div className="titleRow"><Title t="مقرراتك" s="تابع تقدمك في كل مقرر"/><button className="primary" onClick={()=>go('courses')}><Plus/>إضافة مقرر جديد</button></div><div className="courseRows">{courses.map((c,i)=>{const p=c.chapters?Math.round(c.done/c.chapters*100):0;return <div className="courseProgress" key={c.id}><div className={'courseIcon ci'+i%4}><FileText/></div><div className="grow"><b>{c.name}</b><small>{c.code}</small></div><div className="bar"><i style={{width:p+'%'}}/></div><b>{p}%</b><span>{c.done}/{c.chapters} شابتر</span></div>})}</div></section><section className="panel history"><div className="titleRow"><Title t="سجل الشباتر المضافة" s="آخر نشاطاتك الدراسية"/><History/></div>{courses.slice(0,5).map((c,i)=><div className="historyRow" key={c.id}><FileText/><div><b>Chapter {Math.max(1,c.done)} - {c.name}</b><small>{c.code}</small></div><span>{i===0?'اليوم':`${i+1} أيام`}</span></div>)}</section></div></>}
function ExamCards(){const[data,setData]=useState(()=>JSON.parse(localStorage.getItem('novi-exams')||'{}'));function setDate(k,v){const n={...data,[k]:v};setData(n);localStorage.setItem('novi-exams',JSON.stringify(n))}return <section className="examCards">{[['quiz','الاختبارات القصيرة','blue'],['mid','الاختبارات النصفية','purple'],['final','الاختبارات النهائية','red']].map(([k,t,c])=><Exam key={k} title={t} tone={c} value={data[k]} onChange={v=>setDate(k,v)}/>)}</section>}
function Exam({title,tone,value,onChange}){const days=value?Math.max(0,Math.ceil((new Date(value+'T23:59:59')-new Date())/86400000)):null;return <div className={'exam '+tone}><div className="examTitle"><CalendarDays/><b>{title}</b></div><div className="examBody"><label><strong>{value||'--'}</strong><span>اختر التاريخ</span><input type="date" value={value||''} onChange={e=>onChange(e.target.value)}/></label><i/><div><strong>{days===null?'--':days}</strong><span>يوم متبقي</span></div></div></div>}
function Stat({icon:I,n,t,tone=''}){return <div className={'stat '+tone}><I/><div><b>{n}</b><small>{t}</small></div></div>}
function Title({t,s}){return <div className="title"><h2>{t}</h2><p>{s}</p></div>}
function Courses({courses,add,remove}){return <><PageHead title="المقررات" sub="أنشئ مقرراتك بالطريقة التي تناسب تخصصك وخطتك الدراسية." action={<button className="primary" onClick={add}><Plus/>إضافة مقرر</button>}/><div className="cards">{courses.map((c,i)=><article className="courseCard" key={c.id}><button title="حذف المقرر" aria-label={`حذف ${c.name}`} onClick={()=>remove(c.id,c.name)} style={{position:'absolute',top:12,left:12,border:0,background:'transparent',cursor:'pointer',color:'#ef4444',padding:8,zIndex:2}}><Trash2 size={20}/></button><div className={'cover c'+(i%4)}><BookOpen/></div><span>{c.code}</span><h3>{c.name}</h3><p>{c.done} من {c.chapters} شابتر مكتمل</p></article>)}</div></>}
function Tests(){return <><PageHead title="مركز الاختبارات" sub="نظّم مواعيد الاختبارات واستعد لها في مكان واحد."/><ExamCards/></>}
function EmptyPage({title}){return <><PageHead title={title} sub="مساحتك المنظمة لحفظ كل ما تحتاجه للدراسة."/><section className="panel empty"><FolderOpen/><b>ستظهر ملفاتك هنا</b></section></>}
function SettingsPage({dark,setDark}){return <><PageHead title="الإعدادات" sub="خصص NoviStudy ليناسب أسلوب دراستك."/><section className="panel"><Setting t="المظهر" p="التبديل بين الوضع الفاتح والداكن" action={<button className="switch" onClick={()=>setDark(v=>!v)}><i className={dark?'on':''}/></button>}/><Setting t="اللغة" p="العربية · دعم واجهة RTL"/></section></>}
function Setting({t,p,action}){return <div className="setting"><div><b>{t}</b><p>{p}</p></div>{action||<ChevronLeft/>}</div>}
function PageHead({title,sub,action}){return <section className="pageHead"><div><h1>{title}</h1><p>{sub}</p></div>{action}</section>}
